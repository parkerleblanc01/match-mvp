<%= form_with(model: match_donation) do |form| %>
  <% if match_donation.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(match_donation.errors.count, "error") %> prohibited this match_donation from being saved:</h2>

      <ul>
        <% match_donation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= render "layouts/donor_form", form: form %>

  <hr>

  <div>
    <%= form.label :max_amount, style: "display: block" %>
    <%= form.text_field :max_amount %>
  </div>

  <div>
    <%= form.label :active, style: "display: block" %>
    <%= form.check_box :active %>
  </div>

  <div>
    <%= form.label :per_donor_amount, style: "display: block" %>
    <%= form.text_field :per_donor_amount %>
  </div>

  <div>
    <%= form.label :per_donation_amount, style: "display: block" %>
    <%= form.text_field :per_donation_amount %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
